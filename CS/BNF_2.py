print("BNF rule: <StockID> ::= <digit><digit><digit><digit>")
print("BNF rule: <username> ::= <letter><letter><digit><digit>")
index = True

'''
while index == True
    user_input = input("Enter a StockID (6 digits): ")

    if len(user_input) == 6:  

        if user_input.isdigit():   
            print("✅ Valid: This matches the BNF rule for <StockID>")
            index = False

        else:
            print("❌ Invalid: You used non-digit characters")

    else:
        print("❌ Invalid: A StockID must be exactly 4 digits long")
'''

while index == True
    user_input = input("Enter: <code>::= <upper><lower><digit>")

    if len(user_input) == 3:  

        # Unfinished beyond this point
        for i in range(0, 3):
            if user_input[0] ==  

        if user_input.():   
            print("✅ Valid: This matches the BNF rule for code")
            index = False

        else:
            print("❌ Invalid: You used non-digit characters")

    else:
        print("❌ Invalid: A code must be exactly 4 digits long")







